interface:
  display_name: "Better Auth Flutterwave Billing Flows"
  short_description: "Transactions, subscriptions, and billing flows"
  default_prompt: "Use $flutterwave-billing-flows to build verified Flutterwave transaction and subscription flows."
policy:
  allow_implicit_invocation: true
