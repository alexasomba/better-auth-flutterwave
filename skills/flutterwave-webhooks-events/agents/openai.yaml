interface:
  display_name: "Better Auth Flutterwave Webhooks"
  short_description: "Webhook signatures, events, and reconciliation"
  default_prompt: "Use $flutterwave-webhooks-events to implement, debug, or test Flutterwave webhook event processing."
policy:
  allow_implicit_invocation: true
