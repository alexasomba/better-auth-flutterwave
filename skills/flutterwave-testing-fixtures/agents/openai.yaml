interface:
  display_name: "Better Auth Flutterwave Tests"
  short_description: "Focused tests, mocks, and verification"
  default_prompt: "Use $flutterwave-testing-fixtures to choose tests, fixtures, and verification commands for Better Auth Flutterwave changes."
policy:
  allow_implicit_invocation: true
