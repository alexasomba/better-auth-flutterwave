interface:
  display_name: "Better Auth Flutterwave Org Billing"
  short_description: "Organization billing, seats, and access"
  default_prompt: "Use $flutterwave-organization-billing to configure Flutterwave billing for Better Auth organizations, seats, and admin access."
policy:
  allow_implicit_invocation: true
